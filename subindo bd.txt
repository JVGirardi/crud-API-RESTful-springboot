sudo apt update
sudo apt install postgresql postgresql-contrib
sudo service postgresql start
sudo -u postgres psql
ALTER USER postgres PASSWORD 'sua_senha';
\q

commandos úteis
sudo service NOME_DA_DB stop/start/restart/status


melhorias:

customizacao de mensagem, customizacoes de erros, inserir um custom validation,
configurar o exception handler.

incluir paginacao nos métodos de listagem de todos os produtos
incluir filtros para deixar a listagem mais dinâmica
inserir customizacoes de erros
inserir camada service conforme forem surgindo regras de negócios
colocar a camada service e criar os beans do tipo @service